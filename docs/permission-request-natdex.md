# Permission request — CyanSMP64 / NatDexExtension

**Why:** `CyanSMP64/NatDexExtension` has no LICENSE file (GitHub API: `license: null`),
so all rights reserved. Shipping its data tables or sprites, ported or embedded, needs
consent. The randomizer fork is separately GPL-3.0 and needs no permission, only
compliance.

**Where to send it:** open an issue on `github.com/CyanSMP64/NatDexExtension`. Discord
gets faster answers but a GitHub issue gives a public, timestamped, linkable reply,
which is what belongs in `NOTICE`. If he answers on Discord instead, screenshot it and
save it beside this file.

**Sign as yourself, not as Willow Creek Group.** A business signature undercuts the
"no money involved" line.

---

**Subject:** Permission to use Nat. Dex Extension data in a free Android IronMON app

Hi,

I'm building a free, open source Android app that puts the whole GBA IronMON setup in
one place: emulator, tracker and randomizer together, so people can run Kaizo without
a laptop. It will be GPL-3.0 with public source.

I'd like your permission for one thing. NatDexExtension has no license file, so I'm
treating it as all rights reserved rather than assuming. I'd like to use the
extension's Pokémon and move data, and its sprites, so the tracker works properly on
Nat. Dex 1.2.x. Whether I port that data to Kotlin or bundle it as is, it's your work
either way, so I'd rather ask than find out later.

To be clear about what I'm not doing: no ROMs are bundled, distributed or linked.
Users patch their own legally obtained dump with your published BPS, exactly as they
do on PC. It isn't going on the Play Store and there's no money involved.

You'd be credited by name and linked in both the app and the repo. Happy to work to
whatever conditions you want, or to leave Nat. Dex support out entirely if you'd
prefer.

Separately, and not a request: I'm also using your randomizer fork, which is GPL-3.0,
so I'll just comply with the license. Mentioning it so it isn't a surprise.

A one line yes is plenty.

Thanks for building it. Nat. Dex is what makes the challenge worth playing.

Blake Porterfield
